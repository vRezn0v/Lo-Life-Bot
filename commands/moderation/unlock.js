module.exports = {
    name: 'unlock',
    description: 'Unlocks A Previously Locked Channel (To Be Used From Within The Channel To Be Locked)',
    usage: `**Requires Elevated Access**\`\`\`unlock\`\`\``,
    run: async (client, message, args) => {
        // Unlock Flow Here
    }
}